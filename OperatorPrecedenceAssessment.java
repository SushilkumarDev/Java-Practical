public class OperatorPrecedenceAssessment {
    public static void main(String args[]) {
        int length, breadth;
        double perimeter = 0.0;
        length = 10;
        breadth = 5;
        perimeter = (length + breadth)*2;
        // perimeter = (length + breadth)* 2 ;
        System.out.println("The perimeter of the rectangle is = " + perimeter);
    }
}